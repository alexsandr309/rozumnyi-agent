# 🔧 Налаштування Replit API для Автоматизації

Для автоматичного налаштування проєкту на Replit мені потрібен **Replit API Token**.

## 📋 Як отримати Replit API Token

### Крок 1: Відкрийте Replit Dashboard

1. Перейдіть до [Replit Dashboard](https://replit.com/)
2. Увійдіть у свій акаунт

### Крок 2: Створіть API Token

1. Натисніть на ваш **профіль** (аватар у правому верхньому куті)
2. Виберіть **"Account"** або **"Settings"**
3. Перейдіть до розділу **"API Tokens"** або **"Developer"**
4. Натисніть **"Create Token"** або **"Generate new token"**
5. Заповніть:
   - **Name**: `render-auto-setup` (або будь-яка назва)
   - **Scopes**: Виберіть всі необхідні права (мінімум: `repls:read`, `repls:write`, `secrets:read`, `secrets:write`)
6. Натисніть **"Create"** або **"Generate"**
7. **ВАЖЛИВО**: Скопіюйте токен одразу (показується тільки один раз!)

### Альтернативний шлях (якщо не знайдено в Settings)

1. Перейдіть до [Replit API Documentation](https://docs.replit.com/api-reference/introduction)
2. Перевірте розділ "Authentication"
3. Можливо, потрібно використати **Replit CLI** для генерації токена:
   ```bash
   replit auth
   ```

## 🔑 Що робити з токеном

Додайте токен у файл `auto_config.json`:

```json
{
  "replit": {
    "api_token": "ваш_replit_api_token",
    "username": "ваш_replit_username"
  }
}
```

## ⚠️ Важливо

- **Не публікуйте токен** у публічних репозиторіях
- Токен дає доступ до вашого Replit акаунту
- Зберігайте токен в безпеці
- Якщо токен скомпрометовано, негайно видаліть його та створіть новий

## 📚 Додаткова інформація

- [Replit API Documentation](https://docs.replit.com/api-reference/introduction)
- [Replit API Authentication](https://docs.replit.com/api-reference/authentication)

---

**Після отримання токена**: Додайте його в `auto_config.json` та запустіть `python auto_setup.py`

