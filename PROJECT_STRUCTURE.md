# 📁 Структура Проєкту

Опис файлів та їх призначення.

## 📂 Файли проєкту

```
розумний агент/
├── app.py                          # Головний файл додатку
├── requirements.txt                # Python залежності
├── env.example                     # Приклад змінних оточення
├── render.yaml                     # Конфігурація для Render
├── .gitignore                      # Файли для ігнорування Git
├── README.md                       # Основна документація
├── QUICK_START.md                  # Швидкий старт
├── GOOGLE_DRIVE_SETUP.md           # Налаштування Google Drive API
└── PROJECT_STRUCTURE.md            # Цей файл
```

## 📄 Опис файлів

### `app.py`
Головний файл додатку, що містить:
- **Flask веб-сервер** з Gunicorn
- **Keep-Alive ендпоінт** (`/`) для UptimeRobot
- **Фоновий потік** для AI-агента
- **Reinforcement Learning агент** (Actor-Critic)
- **Супервізор** з контролем ризику
- **Інтеграція з Google Drive API** для хмарної пам'яті
- **Інтеграція з Binance Testnet** через ccxt
- **Технічний та фундаментальний аналіз**

### `requirements.txt`
Python залежності:
- Flask + Gunicorn (веб-сервер)
- ccxt (API бірж)
- pandas, numpy (обробка даних)
- tensorflow (RL агент)
- google-api-python-client (Google Drive)

### `env.example`
Приклад файлу змінних оточення. Скопіюйте в `.env` та заповніть:
- API ключі Binance Testnet
- Режим торгівлі (CONSERVATIVE/BALANCED/AGGRESSIVE)
- Параметри навчання та торгівлі

### `render.yaml`
Blueprint для автоматичного розгортання на Render. Містить:
- Конфігурацію веб-сервісу
- Build та Start команди
- Базові environment variables

### `.gitignore`
Файли, які не потрібно комітити в Git:
- `.env` (секретні дані)
- `client_secrets.json`, `token.json` (Google API)
- `*.keras`, `*.h5` (моделі)
- `__pycache__/`, `*.pyc` (кеш Python)

### `README.md`
Основна документація з:
- Описом проєкту
- Вимогами
- Інструкціями по встановленню
- Описом API ендпоінтів
- Troubleshooting

### `QUICK_START.md`
Покрокові інструкції для швидкого запуску:
- Локальне тестування
- Розгортання на Render
- Налаштування UptimeRobot
- Перевірка роботи

### `GOOGLE_DRIVE_SETUP.md`
Детальні інструкції для налаштування Google Drive API:
- Створення проєкту в Google Cloud Console
- Увімкнення API
- Створення OAuth credentials
- Налаштування для Render

## 🏗️ Архітектура коду

### Модулі в `app.py`

1. **Configuration & Logging**
   - Налаштування змінних оточення
   - Логування

2. **Risk Management**
   - `RiskBand` enum (режими ризику)
   - `Supervisor` (AI супервізор)

3. **CloudModelManager**
   - Завантаження/збереження моделі з Google Drive

4. **MarketDataFetcher**
   - Отримання OHLCV даних
   - Технічний аналіз (RSI, MA, волатильність)
   - Фундаментальний аналіз (книга ордерів)

5. **TradeExecutor**
   - Виконання торгових операцій
   - Контроль балансу

6. **TradingEnvironment**
   - Середовище для RL агента
   - Формування стану ринку

7. **ActorCriticAgent**
   - RL агент (Actor-Critic архітектура)
   - Навчання на досвіді

8. **TradingAgentRunner**
   - Головний цикл торгівлі
   - Оркестрація всіх компонентів

9. **Flask App**
   - Веб-сервер
   - API ендпоінти
   - Фоновий потік

## 🔄 Потік даних

```
UptimeRobot → Flask (/)
                ↓
         Background Thread
                ↓
    TradingAgentRunner.run()
                ↓
    ┌───────────┴───────────┐
    ↓                        ↓
Environment.get_state()  Supervisor.evaluate()
    ↓                        ↓
MarketDataFetcher      RiskBand adjustment
    ↓                        ↓
    └───────────┬───────────┘
                ↓
    ActorCriticAgent.select_action()
                ↓
    TradeExecutor.execute()
                ↓
    Agent.train_batch()
                ↓
    CloudModelManager.save()
```

## 📊 Ключові компоненти

### Keep-Alive механізм
- UptimeRobot пінгує `/` кожні 5 хвилин
- Запобігає "засинанню" безкоштовного Render інстансу

### Фоновий потік
- Торговий цикл працює незалежно від веб-сервера
- Не блокує Keep-Alive запити

### Хмарна пам'ять
- Модель зберігається на Google Drive
- Автоматичне завантаження при старті
- Періодичне збереження під час роботи

### Дворівнева безпека
1. **Рівень користувача**: вибір базового режиму ризику
2. **Рівень AI**: автоматичне зниження ризику при небезпеці

## 🎯 Наступні кроки розвитку

Можливі покращення:
- [ ] Додати більше технічних індикаторів
- [ ] Покращити оцінку винагороди (PnL tracking)
- [ ] Додати backtesting модуль
- [ ] Створити веб-дашборд для моніторингу
- [ ] Додати Telegram бот для сповіщень
- [ ] Реалізувати multi-symbol стратегії

---

**Примітка**: Ця структура оптимізована для розгортання на Render Free Tier з мінімальними вимогами.

