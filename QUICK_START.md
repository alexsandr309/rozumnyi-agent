# ⚡ Швидкий Старт

Покрокові інструкції для швидкого запуску AI-агента.

## 📋 Чеклист перед стартом

- [ ] Python 3.9+ встановлено
- [ ] Google Cloud Console проєкт створено
- [ ] Google Drive API увімкнено
- [ ] `client_secrets.json` завантажено
- [ ] Binance Testnet API ключі отримано
- [ ] Render акаунт створено
- [ ] UptimeRobot акаунт створено

## 🚀 Крок 1: Локальне тестування (5 хвилин)

### 1.1 Встановлення залежностей

```bash
cd "розумний агент"
pip install -r requirements.txt
```

### 1.2 Налаштування змінних оточення

Створіть файл `.env`:

```bash
# Windows
copy env.example .env

# Linux/Mac
cp env.example .env
```

Відредагуйте `.env`:
- Додайте `BINANCE_TESTNET_API_KEY`
- Додайте `BINANCE_TESTNET_SECRET_KEY`
- Виберіть `TRADING_MODE` (CONSERVATIVE, BALANCED, або AGGRESSIVE)

### 1.3 Налаштування Google Drive

1. Покладіть `client_secrets.json` у папку проєкту
2. Запустіть проєкт:
   ```bash
   python app.py
   ```
3. При першому запуску з'явиться URL для авторизації
4. Відкрийте URL у браузері та авторизуйтеся
5. Після авторизації створюється `token.json`

### 1.4 Перевірка роботи

Відкрийте новий термінал:

```bash
# Перевірка Keep-Alive ендпоінту
curl http://localhost:8000/

# Перевірка статусу
curl http://localhost:8000/status

# Перевірка супервізора
curl http://localhost:8000/supervisor
```

Якщо все працює, переходимо до розгортання на Render.

## 🌐 Крок 2: Розгортання на Render (10 хвилин)

### 2.1 Підготовка GitHub репозиторію

1. Створіть новий репозиторій на GitHub
2. Завантажте код (не забудьте `.gitignore`):
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/your-repo.git
   git push -u origin main
   ```

### 2.2 Створення Web Service на Render

1. Увійдіть до [Render Dashboard](https://dashboard.render.com/)
2. Натисніть **"New +"** → **"Web Service"**
3. Підключіть ваш GitHub репозиторій
4. Налаштування:
   - **Name**: `розумний-агент`
   - **Root Directory**: `розумний агент` (якщо проєкт в підпапці)
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app --bind 0.0.0.0:$PORT`
   - **Plan**: Free

### 2.3 Налаштування Environment Variables

У Render Dashboard → ваш сервіс → **Environment**:

Додайте всі змінні:

```
BINANCE_TESTNET_API_KEY=your_key
BINANCE_TESTNET_SECRET_KEY=your_secret
TRADING_MODE=BALANCED
MODEL_FILE_NAME=agent_model.keras
CLIENT_SECRETS_FILE=client_secrets.json
GOOGLE_TOKEN_FILE=token.json
POLL_INTERVAL_SECONDS=60
SAVE_MODEL_INTERVAL_STEPS=30
TRAIN_BATCH_SIZE=32
LOG_LEVEL=INFO
```

### 2.4 Завантаження Secret Files

1. У Render Dashboard → ваш сервіс → **Environment**
2. Прокрутіть до **"Secret Files"**
3. Додайте `client_secrets.json`:
   - Відкрийте локальний `client_secrets.json`
   - Скопіюйте весь вміст
   - У Render: **Name**: `client_secrets.json`
   - **Contents**: вставте вміст

4. Додайте `token.json` (якщо він вже існує після локальної авторизації):
   - Аналогічно до `client_secrets.json`

### 2.5 Перший деплой

1. Натисніть **"Save Changes"**
2. Render автоматично почне деплой
3. Перевірте логи: Render Dashboard → ваш сервіс → **"Logs"**

### 2.6 Отримання URL

Після успішного деплою ви отримаєте URL:
```
https://your-app-name.onrender.com
```

## 🤖 Крок 3: Налаштування UptimeRobot (2 хвилини)

1. Увійдіть до [UptimeRobot](https://uptimerobot.com/)
2. Натисніть **"Add New Monitor"**
3. Налаштування:
   - **Monitor Type**: HTTP(s)
   - **Friendly Name**: Розумний Агент
   - **URL**: `https://your-app-name.onrender.com/`
   - **Monitoring Interval**: 5 minutes
   - **Alert Contacts**: ваш email
4. Натисніть **"Create Monitor"**

UptimeRobot буде пінгувати ваш сервіс кожні 5 хвилин, запобігаючи "засинанню".

## ✅ Крок 4: Перевірка роботи

### 4.1 Перевірка Keep-Alive

```bash
curl https://your-app-name.onrender.com/
```

Очікуваний результат:
```json
{
  "status": "ok",
  "timestamp": "2024-01-15T12:00:00Z",
  "agent_running": true,
  "step_counter": 42
}
```

### 4.2 Перевірка статусу

```bash
curl https://your-app-name.onrender.com/status
```

### 4.3 Перевірка логів

Render Dashboard → ваш сервіс → **"Logs"**

Шукайте:
- `"Фоновий агент запущено"`
- `"Старт торгового циклу"`
- `"Крок X | BTC/USDT | buy | винагорода=..."`

## 🎯 Наступні кроки

1. **Моніторинг**: Перевіряйте логи регулярно
2. **Оптимізація**: Налаштуйте параметри в Environment Variables
3. **Модель**: Після першого збереження моделі додайте `MODEL_DRIVE_FILE_ID`
4. **Аналітика**: Використовуйте API ендпоінти для моніторингу

## 🐛 Якщо щось не працює

### Render сервіс не запускається
- Перевірте логи на Render
- Перевірте правильність `Start Command`
- Перевірте, що всі залежності встановлено

### Помилка з Google Drive
- Перевірте `client_secrets.json` у Secret Files
- Перевірте, що Google Drive API увімкнено
- Перевірте логи на помилки авторизації

### Агент не торгує
- Перевірте Binance Testnet API ключі
- Перевірте баланс на Testnet
- Перевірте логи на помилки API

### UptimeRobot не працює
- Перевірте правильність URL
- Перевірте, що сервіс доступний
- Перевірте налаштування моніторингу

---

**Готово!** 🎉 Ваш AI-агент тепер працює автономно 24/7 на Render.

