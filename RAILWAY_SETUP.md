# 🚂 Налаштування Railway.app

## ✅ Що вже зроблено автоматично:

1. ✅ Проект створено: `розумний-агент`
2. ✅ Сервіс створено: `rozumnyi-agent`
3. ✅ GitHub репозиторій підключено
4. ✅ `Procfile` створено для автоматичного деплою

## 📋 Що потрібно зробити вручну:

### Крок 1: Встановити Environment Variables

1. Перейдіть на https://railway.app/project/[ваш-project-id]
2. Виберіть сервіс **`rozumnyi-agent`**
3. Перейдіть у вкладку **Variables** (або **Settings** → **Variables**)
4. Додайте наступні змінні:

#### Обов'язкові змінні:

```
BINANCE_TESTNET_API_KEY = eIJacGfSEZYaT5kx6UuvNY9WQlBbuG1J53PYfIowtFUO4zlNOR5HBfvIYtMDoaz
BINANCE_TESTNET_SECRET_KEY = yO68gvmqppkUVzUzaaANpc76vNfZfTaP4Rn0O3MkMZlCINaGaZTz5Dkqmougs4Ly
TRADING_MODE = BALANCED
DEPOSIT_USDT = 1000
```

#### Додаткові змінні (мають значення за замовчуванням):

```
MODEL_FILE_NAME = agent_model.keras
CLIENT_SECRETS_FILE = service_account.json
GOOGLE_TOKEN_FILE = token.json
POLL_INTERVAL_SECONDS = 60
SAVE_MODEL_INTERVAL_STEPS = 100
TRAIN_BATCH_SIZE = 64
LOG_LEVEL = INFO
```

### Крок 2: Завантажити service_account.json

1. У тому ж розділі **Variables**
2. Знайдіть секцію **Secret Files** або **Files**
3. Натисніть **Add Secret File**
4. Назва файлу: `service_account.json`
5. Вставте вміст вашого файлу `service_account.json` з локального проєкту

### Крок 3: Перевірити деплой

1. Після встановлення змінних Railway автоматично запустить деплой
2. Перейдіть у вкладку **Deployments** щоб побачити прогрес
3. Перевірте логи у вкладці **Logs** якщо є помилки

### Крок 4: Отримати URL додатку

1. Після успішного деплою перейдіть у **Settings** сервісу
2. Знайдіть секцію **Domains** або **Networking**
3. Railway автоматично створить URL типу: `https://rozumnyi-agent-production.up.railway.app`

## 🔍 Перевірка роботи

Після деплою перевірте:

1. **Keep-Alive ендпоінт**: `curl https://your-app.railway.app/`
2. **Статус**: `curl https://your-app.railway.app/status`
3. **Логи**: Перевірте вкладку **Logs** на Railway

## ⚠️ Важливо

- Після налаштування **видаліть** файл `auto_config.json` з репозиторію (він вже в `.gitignore`)
- Не комітьте `service_account.json` в Git (він вже в `.gitignore`)
- Railway автоматично деплоїть код при кожному push в GitHub

## 🆘 Якщо щось не працює

1. Перевірте логи в Railway Dashboard → Logs
2. Перевірте, чи всі змінні встановлені правильно
3. Перевірте, чи `service_account.json` завантажено як Secret File
4. Переконайтеся, що GitHub репозиторій правильно підключено

