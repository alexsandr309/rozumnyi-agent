# 🧠 Розумний Агент - Генеративний AI-Агент для Торгівлі Криптовалютою

Автономний AI-агент для торгівлі криптовалютою на спотовому ринку, розгорнутий на Render з хмарною пам'яттю через Google Drive.

## 🎯 Особливості

- **Автономна робота 24/7** на Render (Free Tier)
- **Reinforcement Learning** агент для самостійного навчання
- **Дворівнева система безпеки** з AI-супервізором
- **Хмарна пам'ять** через Google Drive API
- **Keep-Alive механізм** для UptimeRobot
- **Тільки спотова торгівля** (без ф'ючерсів та кредитного плеча)

## 📋 Вимоги

- Python 3.9+
- Google Cloud Console проект з увімкненим Google Drive API
- Binance Testnet API ключі
- Render акаунт (Free Tier достатньо)
- UptimeRobot акаунт (Free Plan)

## 🚀 Швидкий Старт

### 1. Клонування та встановлення залежностей

```bash
cd "розумний агент"
pip install -r requirements.txt
```

### 2. Налаштування Google Drive API

1. Перейдіть до [Google Cloud Console](https://console.cloud.google.com/)
2. Створіть новий проект або виберіть існуючий
3. Увімкніть **Google Drive API**
4. Створіть **OAuth 2.0 Client ID** (тип: Desktop app)
5. Завантажте `client_secrets.json` та покладіть у папку проєкту

### 3. Налаштування Binance Testnet

1. Зареєструйтеся на [Binance Testnet](https://testnet.binancefuture.com/)
2. Створіть API ключі
3. Додайте їх у `.env` файл

### 4. Створення `.env` файлу

Скопіюйте `.env.example` в `.env` та заповніть:

```bash
cp .env.example .env
```

Відредагуйте `.env`:
- `BINANCE_TESTNET_API_KEY` - ваш API ключ
- `BINANCE_TESTNET_SECRET_KEY` - ваш секретний ключ
- `TRADING_MODE` - виберіть режим: `CONSERVATIVE`, `BALANCED`, або `AGGRESSIVE`

### 5. Локальне тестування

```bash
python app.py
```

Сервер запуститься на `http://localhost:8000`

Перевірте Keep-Alive ендпоінт:
```bash
curl http://localhost:8000/
```

## 🌐 Розгортання на Render

### 1. Підготовка проєкту

1. Завантажте проєкт на GitHub
2. Увійдіть до [Render Dashboard](https://dashboard.render.com/)

### 2. Створення Web Service

1. Натисніть **"New +"** → **"Web Service"**
2. Підключіть ваш GitHub репозиторій
3. Налаштування:
   - **Name**: `розумний-агент` (або будь-яка назва)
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app --bind 0.0.0.0:$PORT`
   - **Plan**: Free

### 3. Налаштування Environment Variables

У Render Dashboard → ваш сервіс → **Environment**:

Додайте всі змінні з `.env` файлу:
- `BINANCE_TESTNET_API_KEY`
- `BINANCE_TESTNET_SECRET_KEY`
- `TRADING_MODE`
- `MODEL_DRIVE_FILE_ID` (залиште порожнім, заповниться автоматично)
- `CLIENT_SECRETS_FILE=client_secrets.json`
- `GOOGLE_TOKEN_FILE=token.json`
- Інші параметри з `.env.example`

### 4. Завантаження `client_secrets.json`

Render не підтримує завантаження файлів через UI. Варіанти:

**Варіант A: Через Environment Variable (рекомендовано)**
- Конвертуйте `client_secrets.json` в base64:
  ```bash
  cat client_secrets.json | base64
  ```
- Додайте змінну `GOOGLE_CLIENT_SECRETS_B64` з base64 значенням
- Модифікуйте `app.py` для декодування (або використайте секрети Render)

**Варіант B: Через Build Script**
- Додайте скрипт, який створює `client_secrets.json` з environment variable під час build

### 5. Налаштування UptimeRobot

1. Зареєструйтеся на [UptimeRobot](https://uptimerobot.com/) (Free Plan)
2. Створіть новий **HTTP(s) Monitor**:
   - **URL**: `https://your-render-app.onrender.com/`
   - **Interval**: 5 minutes
   - **Alert Contacts**: ваш email

UptimeRobot буде пінгувати ваш сервіс кожні 5 хвилин, запобігаючи "засинанню" безкоштовного інстансу Render.

## 🛡️ Система Безпеки

### Режими Ризику

1. **CONSERVATIVE** (Консервативний)
   - Максимум 10% депозиту в торгівлі
   - AI не може змінювати режим

2. **BALANCED** (Збалансований)
   - Максимум 20% депозиту
   - AI може знизити до BALANCED_CONSERVATIVE або CONSERVATIVE

3. **AGGRESSIVE** (Агресивний)
   - Максимум 30% депозиту (70% залишається в резерві)
   - AI може динамічно знижувати ризик залежно від ринкових умов

### AI Супервізор

Супервізор автоматично оцінює:
- **Волатильність ринку** (vol_ratio)
- **Дисбаланс книги ордерів** (book_pressure)
- **Силу тренду** (trend_strength)

При високому "шумі" на ринку супервізор автоматично знижує ризик.

## 📊 API Ендпоінти

- `GET /` - Keep-Alive ендпоінт для UptimeRobot
- `GET /supervisor` - Інформація про поточний режим ризику
- `GET /status` - Детальний статус системи
- `POST /config` - Оновлення конфігурації
- `POST /shutdown` - Зупинка агента

## 🔧 Архітектура

```
app.py
├── Flask Web Server (Gunicorn)
│   └── Keep-Alive ендпоінт (/)
├── Background Thread
│   └── TradingAgentRunner
│       ├── ActorCriticAgent (RL)
│       ├── TradingEnvironment
│       ├── MarketDataFetcher (TA + FA)
│       ├── TradeExecutor
│       └── Supervisor (Risk Management)
└── CloudModelManager
    └── Google Drive API
```

## 📝 Логування

Логи доступні через Render Dashboard → ваш сервіс → **Logs**.

Рівні логування:
- `DEBUG` - детальна інформація
- `INFO` - основні події
- `WARNING` - попередження
- `ERROR` - помилки

## ⚠️ Важливі Зауваження

1. **Testnet тільки**: Проєкт налаштований на Binance Testnet. Для реальної торгівлі потрібні додаткові зміни.

2. **Google Drive токен**: При першому запуску потрібна авторизація через OAuth. На Render це може вимагати додаткових налаштувань.

3. **Безкоштовний Render**: Free Tier має обмеження на час бездіяльності. UptimeRobot допомагає, але не гарантує 100% uptime.

4. **Модель навчання**: RL агент навчається на реальних даних. Перші кроки можуть бути неефективними.

## 🐛 Troubleshooting

### Помилка 403/451 від Binance
- Перевірте, що використовується Testnet API
- Перевірте правильність API ключів

### Модель не завантажується з Google Drive
- Перевірте `MODEL_DRIVE_FILE_ID`
- Перевірте права доступу до файлу на Google Drive
- Перевірте `client_secrets.json` та `token.json`

### Render сервіс "засинає"
- Перевірте налаштування UptimeRobot
- Зменште `POLL_INTERVAL_SECONDS` (але не менше 60 секунд)

## 📚 Додаткові Ресурси

- [CCXT Documentation](https://docs.ccxt.com/)
- [TensorFlow RL Tutorial](https://www.tensorflow.org/agents)
- [Render Documentation](https://render.com/docs)
- [Google Drive API](https://developers.google.com/drive/api)

## 📄 Ліцензія

Цей проєкт створено для навчальних цілей. Використовуйте на свій ризик.

---

**Увага**: Це тестовий проєкт. Не використовуйте реальні кошти без повного тестування та розуміння ризиків.

