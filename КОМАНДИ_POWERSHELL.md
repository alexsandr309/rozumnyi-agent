# 💻 Команди PowerShell - Просто Скопіюйте та Виконайте

## 🚀 Швидкий Старт (Всі команди разом)

Відкрийте PowerShell у папці `розумний агент` та виконайте:

```powershell
# Перехід до папки проєкту (замініть шлях на ваш)
cd "I:\РОБОТЫ ТОРГОВЫЕ\БОТ 080325\archive\розумний агент"

# Перевірка Python
python --version

# Встановлення залежностей
python -m pip install --upgrade pip
python -m pip install -r requirements.txt

# Запуск автоматичного налаштування
python auto_setup.py
```

---

## 📋 Покрокові Команди

### Крок 1: Перехід до папки проєкту

```powershell
cd "I:\РОБОТЫ ТОРГОВЫЕ\БОТ 080325\archive\розумний агент"
```

**Або якщо ви вже в папці archive:**

```powershell
cd "розумний агент"
```

### Крок 2: Перевірка Python

```powershell
python --version
```

**Очікуваний результат:** `Python 3.9.x` або вище

**Якщо помилка:** Встановіть Python з [python.org](https://www.python.org/downloads/)

### Крок 3: Встановлення залежностей

```powershell
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

**Це встановить:**
- Flask, gunicorn
- ccxt (торгівля)
- pandas, numpy
- tensorflow (AI)
- google-api-python-client (Google Drive)
- requests

### Крок 4: Створення конфігурації (якщо ще не створено)

```powershell
# Копіювання прикладу
Copy-Item "auto_config.example.json" "auto_config.json"

# Відкрити для редагування (замініть notepad на ваш редактор)
notepad auto_config.json
```

**Або через PowerShell:**

```powershell
# Створити файл з прикладом
Get-Content "auto_config.example.json" | Set-Content "auto_config.json"
notepad auto_config.json
```

### Крок 5: Перевірка файлів

```powershell
# Перевірка наявності файлів
Test-Path "auto_config.json"
Test-Path "service_account.json"
Test-Path "app.py"
```

### Крок 6: Запуск автоматичного налаштування

```powershell
python auto_setup.py
```

---

## 🎯 Альтернатива: Використання скрипта

Якщо хочете автоматично виконати всі кроки:

```powershell
# Перехід до папки
cd "I:\РОБОТЫ ТОРГОВЫЕ\БОТ 080325\archive\розумний агент"

# Запуск скрипта
.\setup.ps1
```

**Якщо отримаєте помилку про виконання скриптів:**

```powershell
# Дозволити виконання скриптів (тільки для поточного сеансу)
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process

# Потім запустіть скрипт
.\setup.ps1
```

---

## 🔧 Корисні Команди

### Перевірка встановлених пакетів

```powershell
pip list
```

### Перевірка версії Python

```powershell
python --version
```

### Перевірка поточної папки

```powershell
Get-Location
# або
pwd
```

### Перегляд вмісту папки

```powershell
Get-ChildItem
# або
ls
# або
dir
```

### Перегляд вмісту файлу

```powershell
Get-Content "auto_config.json"
# або
cat "auto_config.json"
```

### Створення файлу

```powershell
# Створити порожній файл
New-Item "test.txt" -ItemType File

# Створити файл з текстом
"Текст" | Out-File "test.txt" -Encoding UTF8
```

---

## 🐛 Troubleshooting

### Помилка "python не распознан"

```powershell
# Перевірте, чи Python в PATH
$env:Path -split ';' | Select-String python

# Або спробуйте
py --version
```

### Помилка встановлення пакетів

```powershell
# Оновіть pip
python -m pip install --upgrade pip --user

# Спробуйте встановити окремо
python -m pip install requests
```

### Помилка "не удается найти модуль"

```powershell
# Перевірте встановлені пакети
pip list | Select-String "requests"

# Перевстановіть
python -m pip install --force-reinstall requests
```

### Помилка з кодуванням (кирилиця)

```powershell
# Встановіть кодування UTF-8
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
```

---

## ✅ Перевірка Після Налаштування

```powershell
# Перевірка, що все встановлено
python -c "import flask, ccxt, pandas, numpy, tensorflow; print('Всі модулі встановлено!')"

# Перевірка конфігурації
python -c "import json; print(json.load(open('auto_config.json')))"
```

---

## 📝 Приклад Повної Послідовності

```powershell
# 1. Перехід до папки
cd "I:\РОБОТЫ ТОРГОВЫЕ\БОТ 080325\archive\розумний агент"

# 2. Перевірка Python
python --version

# 3. Встановлення залежностей
python -m pip install --upgrade pip
python -m pip install -r requirements.txt

# 4. Створення конфігурації (якщо потрібно)
if (-not (Test-Path "auto_config.json")) {
    Copy-Item "auto_config.example.json" "auto_config.json"
    Write-Host "Відредагуйте auto_config.json та додайте ваші credentials!"
    notepad auto_config.json
}

# 5. Запуск налаштування
python auto_setup.py
```

---

**Готово!** Просто скопіюйте команди та виконайте їх у PowerShell! 🚀

