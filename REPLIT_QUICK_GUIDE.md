# ⚡ Швидкий Гайд: Replit API Token

## 🎯 Що потрібно

Для автоматичного налаштування на Replit мені потрібен **Replit API Token**.

## 📋 Як отримати (3 кроки)

### Крок 1: Відкрийте Replit Dashboard

1. Перейдіть до [replit.com](https://replit.com/)
2. Увійдіть у свій акаунт

### Крок 2: Знайдіть API Tokens

**Варіант A (стандартний):**
1. Натисніть на ваш **аватар** (правый верхній кут)
2. Виберіть **"Account"** або **"Settings"**
3. Шукайте розділ **"API Tokens"** або **"Developer"**

**Варіант B (якщо не знайдено):**
1. Перейдіть до [Replit API Documentation](https://docs.replit.com/api-reference/introduction)
2. Перевірте розділ "Authentication"
3. Можливо, потрібно використати **Replit CLI**:
   ```bash
   replit auth
   ```

### Крок 3: Створіть токен

1. Натисніть **"Create Token"** або **"Generate new token"**
2. Заповніть:
   - **Name**: `render-auto-setup`
   - **Scopes**: Виберіть мінімум:
     - ✅ `repls:read` - читання Repls
     - ✅ `repls:write` - створення/редагування Repls
     - ✅ `secrets:read` - читання Secrets
     - ✅ `secrets:write` - створення/редагування Secrets
3. Натисніть **"Create"**
4. **ВАЖЛИВО**: Скопіюйте токен одразу! (показується тільки один раз)

## 📝 Додайте в auto_config.json

```json
{
  "platform": "replit",
  "replit": {
    "api_token": "ваш_скопійований_токен",
    "username": "ваш_replit_username",
    "repl_name": "розумний-агент"
  }
}
```

## 🔍 Де знайти username?

Ваш Replit username видно:
- У URL вашого профілю: `https://replit.com/@ваш_username`
- У профілі після символу `@`

## ⚠️ Важливо

- **Не публікуйте токен** у публічних репозиторіях
- Токен дає доступ до вашого Replit акаунту
- Зберігайте токен в безпеці
- Якщо токен скомпрометовано, негайно видаліть його

## 🚀 Після отримання токена

1. Додайте токен у `auto_config.json`
2. Встановіть `"platform": "replit"` у `auto_config.json`
3. Запустіть `python auto_setup.py`

Скрипт автоматично:
- ✅ Створить Repl на Replit
- ✅ Завантажить всі файли
- ✅ Налаштує Secrets
- ✅ Готово!

---

**Детальні інструкції**: Див. `REPLIT_SETUP.md`

