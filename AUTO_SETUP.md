# 🤖 Автоматичне Налаштування - Повний Контроль

Цей документ описує, як надати AI-асистенту повний контроль для автоматичного налаштування проєкту.

## 📋 Що потрібно для повної автоматизації

### 1. Доступ до Google Cloud Console
- **Що потрібно**: API ключі та credentials
- **Як надати**: Створіть Service Account з правами на Google Drive

### 2. Доступ до Binance Testnet
- **Що потрібно**: API ключі
- **Як надати**: Створіть API ключі та надайте їх через змінні оточення

### 3. Доступ до GitHub
- **Що потрібно**: Personal Access Token
- **Як надати**: Створіть токен з правами на створення репозиторіїв

### 4. Доступ до Render
- **Що потрібно**: API ключ
- **Як надати**: Створіть API ключ у Render Dashboard

### 5. Доступ до UptimeRobot
- **Що потрібно**: API ключ
- **Як надати**: Створіть API ключ у UptimeRobot

## 🔐 Крок 1: Створення Service Account для Google Drive

Це дозволить автоматично працювати з Google Drive без OAuth.

### 1.1 Створення Service Account

1. Перейдіть до [Google Cloud Console](https://console.cloud.google.com/)
2. Виберіть ваш проєкт
3. Перейдіть до **"IAM & Admin"** → **"Service Accounts"**
4. Натисніть **"Create Service Account"**
5. Заповніть:
   - **Name**: `trading-agent-drive`
   - **Description**: `Service account for trading agent Google Drive access`
6. Натисніть **"Create and Continue"**
7. Надайте роль: **"Editor"** (або **"Storage Admin"**)
8. Натисніть **"Continue"** → **"Done"**

### 1.2 Створення ключа

1. Клікніть на створений Service Account
2. Перейдіть до вкладки **"Keys"**
3. Натисніть **"Add Key"** → **"Create new key"**
4. Виберіть **"JSON"**
5. Завантажте файл та збережіть як `service_account.json`

### 1.3 Надання доступу до Google Drive

1. Відкрийте завантажений `service_account.json`
2. Скопіюйте значення `client_email` (виглядає як `xxx@xxx.iam.gserviceaccount.com`)
3. Відкрийте [Google Drive](https://drive.google.com/)
4. Створіть папку `TradingAgent` (або будь-яку назву)
5. Клікніть правою кнопкою на папку → **"Share"**
6. Вставте `client_email` та надайте права **"Editor"**
7. Натисніть **"Send"**

### 1.4 Збереження credentials

Покладіть `service_account.json` у папку проєкту:
```
розумний агент/service_account.json
```

## 🔑 Крок 2: Отримання API ключів

### 2.1 Binance Testnet API

1. Перейдіть до [Binance Testnet](https://testnet.binancefuture.com/)
2. Увійдіть або зареєструйтеся
3. Перейдіть до **"API Management"**
4. Створіть новий API ключ
5. Скопіюйте:
   - **API Key**
   - **Secret Key**

### 2.2 GitHub Personal Access Token

1. Перейдіть до [GitHub Settings](https://github.com/settings/tokens)
2. Натисніть **"Generate new token"** → **"Generate new token (classic)"**
3. Заповніть:
   - **Note**: `render-auto-setup`
   - **Expiration**: `Custom date` (наприклад, `13.11.2026`)
   - **Scopes**: Відмітьте `repo` (повний контроль репозиторіїв)
4. Натисніть **"Generate token"**
5. Скопіюйте токен (показується тільки один раз!)

### 2.3 Render API Key

1. Перейдіть до [Render Dashboard](https://dashboard.render.com/)
2. Перейдіть до **"Account Settings"** → **"API Keys"**
3. Натисніть **"Create API Key"**
4. Заповніть:
   - **Name**: `render-auto-setup`
5. Скопіюйте API ключ

### 2.4 UptimeRobot API Key

1. Перейдіть до [UptimeRobot API Settings](https://uptimerobot.com/dashboard.php#mySettings)
2. Прокрутіть до **"API Settings"**
3. Скопіюйте **"Main API Key"**

## 📝 Крок 3: Створення конфігураційного файлу

Створіть файл `auto_config.json` у папці проєкту з усіма credentials:

```json
{
  "binance": {
    "testnet_api_key": "ваш_binance_api_key",
    "testnet_secret_key": "ваш_binance_secret_key"
  },
  "github": {
    "username": "ваш_github_username",
    "token": "ваш_github_personal_access_token",
    "repo_name": "розумний-агент"
  },
  "render": {
    "api_key": "ваш_render_api_key"
  },
  "uptimerobot": {
    "api_key": "ваш_uptimerobot_api_key"
  },
  "trading": {
    "mode": "BALANCED",
    "deposit_usdt": 1000
  }
}
```

**⚠️ ВАЖЛИВО**: Цей файл містить секрети! Додайте його до `.gitignore`!

## 🚀 Крок 4: Запуск автоматичного налаштування

Після створення `auto_config.json` та `service_account.json`, запустіть:

```bash
python auto_setup.py
```

Скрипт автоматично:
1. Створить GitHub репозиторій
2. Завантажить код
3. Створить Render сервіс
4. Налаштує Environment Variables
5. Завантажить Secret Files
6. Створить UptimeRobot монітор
7. Запустить деплой

## 🔒 Безпека

### Захист credentials

1. **Ніколи не комітьте** `auto_config.json` у Git
2. Використовуйте `.gitignore` для захисту
3. Після налаштування видаліть `auto_config.json` (credentials будуть на Render)
4. Обмежте права Service Account до мінімуму

### Альтернатива: Environment Variables

Замість `auto_config.json` можна використати змінні оточення:

```bash
export BINANCE_API_KEY="..."
export BINANCE_SECRET_KEY="..."
export GITHUB_TOKEN="..."
# і т.д.
```

## 📞 Підтримка

Якщо виникнуть проблеми:
1. Перевірте логи скрипта
2. Перевірте права доступу
3. Перевірте правильність credentials

---

**Готово!** Після виконання цих кроків AI-асистент зможе автоматично налаштувати весь проєкт.

