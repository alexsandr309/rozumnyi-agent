# 🚀 ПОЧНИ ТУТ - Повне Автоматичне Налаштування

Цей документ містить всі необхідні кроки для надання AI-асистенту повного контролю над налаштуванням проєкту.

## 🎯 Що буде зроблено автоматично

Після виконання цих кроків, AI-асистент автоматично:
1. ✅ Створить GitHub репозиторій
2. ✅ Завантажить код на GitHub
3. ✅ Створить Render Web Service
4. ✅ Налаштує всі Environment Variables
5. ✅ Завантажить Secret Files
6. ✅ Створить UptimeRobot монітор
7. ✅ Запустить деплой

**Ваша участь мінімальна** - тільки отримання API ключів та створення конфігураційного файлу.

---

## 📝 Покрокова Інструкція

### Крок 1: Google Cloud Console (5 хвилин)

1. Перейдіть до [Google Cloud Console](https://console.cloud.google.com/)
2. Створіть новий проєкт або виберіть існуючий
3. Увімкніть **Google Drive API**:
   - "APIs & Services" → "Library" → пошук "Google Drive API" → "Enable"
4. Створіть **Service Account**:
   - "IAM & Admin" → "Service Accounts" → "Create Service Account"
   - Name: `trading-agent-drive`
   - Role: **"Editor"** або **"Storage Admin"**
5. Створіть ключ:
   - Клікніть на створений Service Account → "Keys" → "Add Key" → "Create new key" → **JSON**
   - Завантажте файл та збережіть як `service_account.json` у папку `розумний агент/`
6. Надайте доступ до Google Drive:
   - Відкрийте `service_account.json` та скопіюйте `client_email` (виглядає як `xxx@xxx.iam.gserviceaccount.com`)
   - Відкрийте [Google Drive](https://drive.google.com/)
   - Створіть папку `TradingAgent`
   - Клікніть правою кнопкою → "Share" → вставте `client_email` → права **"Editor"**

**✅ Результат**: Файл `service_account.json` у папці проєкту

---

### Крок 2: Binance Testnet (3 хвилини)

1. Перейдіть до [Binance Testnet](https://testnet.binancefuture.com/)
2. Увійдіть або зареєструйтеся
3. Перейдіть до **"API Management"**
4. Створіть новий API ключ
5. Скопіюйте:
   - **API Key** → збережіть для кроку 6
   - **Secret Key** → збережіть для кроку 6

**✅ Результат**: Два ключі (API Key та Secret Key)

---

### Крок 3: GitHub Personal Access Token (2 хвилини)

1. Перейдіть до [GitHub Settings → Tokens](https://github.com/settings/tokens)
2. Натисніть **"Generate new token"** → **"Generate new token (classic)"**
3. Заповніть:
   - **Note**: `render-auto-setup`
   - **Expiration**: `Custom date` (наприклад, `13.11.2026`)
   - **Scopes**: Відмітьте **`repo`** (повний контроль репозиторіїв)
4. Натисніть **"Generate token"**
5. **ВАЖЛИВО**: Скопіюйте токен одразу (показується тільки один раз!)

**✅ Результат**: GitHub Personal Access Token

---

### Крок 4: Render API Key (2 хвилини)

1. Перейдіть до [Render Dashboard](https://dashboard.render.com/)
2. Увійдіть або зареєструйтеся
3. Перейдіть до **"Account Settings"** → **"API Keys"**
4. Натисніть **"Create API Key"**
5. Name: `render-auto-setup`
6. Скопіюйте API ключ

**✅ Результат**: Render API Key

### Варіант B: Replit

#### Replit API Token (3 хвилини)

1. Перейдіть до [Replit Dashboard](https://replit.com/)
2. Увійдіть у свій акаунт
3. Натисніть на ваш **профіль** (аватар у правому верхньому куті)
4. Виберіть **"Account"** або **"Settings"**
5. Перейдіть до розділу **"API Tokens"** або **"Developer"**
6. Натисніть **"Create Token"** або **"Generate new token"**
7. Заповніть:
   - **Name**: `render-auto-setup`
   - **Scopes**: Виберіть мінімум: `repls:read`, `repls:write`, `secrets:read`, `secrets:write`
8. Натисніть **"Create"**
9. **ВАЖЛИВО**: Скопіюйте токен одразу (показується тільки один раз!)
10. Запишіть ваш **Replit username** (видно у профілі)

**✅ Результат**: Replit API Token та username

**Примітка**: Якщо не знайдете API Tokens у Settings, див. детальні інструкції в `REPLIT_SETUP.md`

---

### Крок 5: UptimeRobot API Key (1 хвилина)

1. Перейдіть до [UptimeRobot](https://uptimerobot.com/)
2. Увійдіть або зареєструйтеся
3. Перейдіть до **"My Settings"** → **"API Settings"**
4. Скопіюйте **"Main API Key"**

**✅ Результат**: UptimeRobot API Key

---

### Крок 6: Створення конфігураційного файлу (2 хвилини)

1. У папці `розумний агент/` знайдіть файл `auto_config.example.json`
2. Скопіюйте його та перейменуйте в `auto_config.json`
3. Відкрийте `auto_config.json` у текстовому редакторі
4. Заповніть всі поля:

```json
{
  "platform": "render",
  "binance": {
    "testnet_api_key": "ВАШ_BINANCE_API_KEY",
    "testnet_secret_key": "ВАШ_BINANCE_SECRET_KEY"
  },
  "github": {
    "username": "ВАШ_GITHUB_USERNAME",
    "token": "ВАШ_GITHUB_TOKEN",
    "repo_name": "розумний-агент"
  },
  "render": {
    "api_key": "ВАШ_RENDER_API_KEY"
  },
  "replit": {
    "api_token": "ВАШ_REPLIT_API_TOKEN",
    "username": "ВАШ_REPLIT_USERNAME",
    "repl_name": "розумний-агент"
  },
  "uptimerobot": {
    "api_key": "ВАШ_UPTIMEROBOT_API_KEY"
  },
  "trading": {
    "mode": "BALANCED",
    "deposit_usdt": 1000
  }
}
```

**Примітки**:
- `platform` може бути `"render"` або `"replit"` (за замовчуванням `"render"`)
- Якщо використовуєте Replit, заповніть секцію `replit`
- Якщо використовуєте Render, заповніть секцію `render`

**Примітки**:
- `repo_name` може бути будь-якою назвою (якщо репозиторій не існує, він буде створений)
- `trading.mode` може бути: `CONSERVATIVE`, `BALANCED`, або `AGGRESSIVE`

**✅ Результат**: Файл `auto_config.json` з усіма credentials

---

### Крок 7: Запуск автоматичного налаштування (5-10 хвилин)

1. Відкрийте термінал у папці `розумний агент/`
2. Встановіть залежності (якщо ще не встановлено):
   ```bash
   pip install -r requirements.txt
   ```
3. Запустіть скрипт автоматичного налаштування:
   ```bash
   python auto_setup.py
   ```

Скрипт автоматично виконає всі необхідні кроки:
- Створення GitHub репозиторію
- Завантаження коду
- Створення Render сервісу
- Налаштування Environment Variables
- Завантаження Secret Files
- Створення UptimeRobot монітора

**⏱️ Час виконання**: 5-10 хвилин

---

### Крок 8: Перевірка (2 хвилини)

Після завершення скрипта:

1. **Перевірте Render Dashboard**:
   - Перейдіть до [Render Dashboard](https://dashboard.render.com/)
   - Знайдіть сервіс `розумний-агент`
   - Перевірте, що деплой успішний (зелений індикатор)

2. **Перевірте логи**:
   - У Render Dashboard → ваш сервіс → "Logs"
   - Шукайте: `"Фоновий агент запущено"`, `"Старт торгового циклу"`

3. **Перевірте Keep-Alive ендпоінт**:
   ```bash
   curl https://your-app-name.onrender.com/
   ```
   Очікуваний результат:
   ```json
   {
     "status": "ok",
     "timestamp": "...",
     "agent_running": true,
     "step_counter": 0
   }
   ```

4. **Перевірте UptimeRobot**:
   - Перейдіть до [UptimeRobot Dashboard](https://uptimerobot.com/dashboard.php)
   - Перевірте, що монітор створено та працює

---

## 🔒 Безпека

**ВАЖЛИВО**: Після успішного налаштування:

1. ✅ **Видаліть файл `auto_config.json`** (credentials вже на Render)
2. ✅ Переконайтеся, що `auto_config.json` додано до `.gitignore`
3. ✅ Не комітьте `service_account.json` у Git (вже в `.gitignore`)

---

## 🐛 Troubleshooting

### Помилка "Service Account не знайдено"
- Перевірте, що `service_account.json` у папці `розумний агент/`
- Перевірте правильність файлу (має бути JSON)

### Помилка "GitHub credentials не знайдено"
- Перевірте правильність GitHub username та token у `auto_config.json`
- Перевірте, що token має права `repo`

### Помилка "Render API key не знайдено"
- Перевірте правильність API ключа у `auto_config.json`
- Перевірте, що ключ активний у Render Dashboard

### Render сервіс не створюється
- Перевірте, що GitHub репозиторій доступний
- Перевірте, що Render має доступ до вашого GitHub акаунту
- Можна створити сервіс вручну через Render Dashboard

### UptimeRobot монітор не створюється
- Перевірте правильність API ключа
- Можна створити монітор вручну після отримання URL сервісу

---

## 📞 Додаткова Допомога

Якщо виникли проблеми:

1. Перевірте логи скрипта `auto_setup.py`
2. Перевірте логи на Render
3. Перевірте детальні інструкції:
   - `AUTO_SETUP.md` - детальний опис процесу
   - `CHECKLIST.md` - чеклист для перевірки
   - `GOOGLE_DRIVE_SETUP.md` - налаштування Google Drive

---

## ✅ Готово!

Після виконання всіх кроків ваш AI-агент буде:
- ✅ Автоматично працювати 24/7 на Render
- ✅ Зберігати модель на Google Drive
- ✅ Моніторитися через UptimeRobot
- ✅ Торгувати на Binance Testnet

**Успіхів! 🚀**

